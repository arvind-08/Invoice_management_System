import React from 'react';
import '../styles.css';  // Import the navbar styles

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1 className="navbar-title">Invoice Management System</h1>
    </nav>
  );
};

export default Navbar;
